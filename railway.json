{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python3 -c 'from app.models.database import init_db; init_db()' && python3 -c 'from app.scripts.seed_historic_circulars import seed_historic_circulars; seed_historic_circulars()' && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}